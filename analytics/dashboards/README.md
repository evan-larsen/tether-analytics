# Dashboard Specs

Create one short file per dashboard before building it in PostHog.

Each dashboard spec should include:

- decisions / questions the dashboard supports
- approved insights
- exact query logic where needed
- known limitations

Only promote a definition into `docs/analytics/metric-registry.md` after it becomes foundational or is reused.
